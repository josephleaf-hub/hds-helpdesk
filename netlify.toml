# Next.js (App Router) build — branch deploy.
# The @netlify/plugin-nextjs adapter handles routing, API route handlers,
# and SSR/ISR. Security headers are set in next.config.mjs.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
